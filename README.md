# Hospital Readmission Prediction App (XGBoost + Streamlit)

This Streamlit application predicts the probability that a patient will be readmitted within 30 days using a trained XGBoost model.

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
